/* FomoCCS Admin TUI — global stylesheet (Textual CSS) */

Screen {
    background: $surface;
}

Header {
    background: $panel;
    color: $text;
    text-style: bold;
    dock: top;
    height: 1;
}

Footer {
    background: $panel;
    dock: bottom;
    height: 1;
}

/* Breadcrumb — shows current screen name */
#breadcrumb {
    dock: top;
    height: 1;
    background: $primary-lighten-1;
    color: $text;
    text-style: bold;
    padding: 0 2;
    border-bottom: solid $primary;
}

/* Source wizard form */
#wizard-form {
    padding: 1 2;
    overflow-y: auto;
    height: 1fr;
}

#wizard-form Label {
    margin-top: 1;
    color: $text-muted;
}

#wizard-form Input {
    width: 50;
    margin-bottom: 1;
}

#wizard-form Select {
    width: 50;
    margin-bottom: 1;
}

#url-row {
    height: auto;
    align: left middle;
}

#url-row Input {
    width: 50;
    margin-right: 2;
    margin-bottom: 0;
}

#wizard-actions {
    margin-top: 2;
    height: auto;
    align: left middle;
}

#wizard-actions Button {
    margin-right: 2;
}

#sw-status {
    margin-top: 1;
    color: $text;
    height: 1;
}

/* Pipeline run screen */
#pipeline-container {
    padding: 1;
}

#pipeline-cmd {
    color: $text-muted;
    padding-bottom: 1;
}

#pipeline-status {
    height: 1;
    padding: 0 2;
    text-style: bold;
}

#pipeline-output {
    height: 1fr;
    overflow-y: auto;
    border: solid $panel;
    padding: 1;
}

#last-crawls {
    height: auto;
    min-height: 3;
    padding: 1 2;
    border: solid $primary;
    background: $surface;
    margin: 1;
}

/* Dashboard grid layout */
#dashboard-grid {
    layout: grid;
    grid-size: 2;
    grid-gutter: 1;
    grid-rows: auto;
    padding: 1;
}

#dashboard-grid Static {
    height: auto;
    padding: 1 2;
    border: solid $primary;
    background: $surface;
    content-align: left top;
}

/* Horizontal filter bar */
#filters {
    height: auto;
    min-height: 3;
    padding: 1;
    background: $panel;
}

#filters Input {
    width: 30;
    margin-right: 2;
    height: auto;
    min-height: 1;
    border: solid $accent;
}

#filters Label {
    padding: 0 2;
    color: $text-muted;
    content-align: center middle;
    height: auto;
    min-height: 1;
}

#counter {
    color: $text-muted;
    text-align: right;
    width: auto;
}

/* DataTables */
DataTable {
    height: 1fr;
    border: solid $panel;
}

DataTable > .datatable--cursor {
    background: $accent 20%;
    color: $text;
}

DataTable > .datatable--header {
    background: $panel;
    color: $text;
    text-style: bold;
}

/* Detail containers */
#source-detail,
#event-detail,
#location-detail,
#operations-container,
#logs-container {
    padding: 1;
}

#source-detail Static,
#event-detail Static,
#location-detail Static {
    padding: 1 0;
}

/* Operations screen */
#operations-container {
    padding: 2;
}

.ops-section {
    border-bottom: solid $accent;
    color: $accent;
    text-style: bold;
    padding: 1 0;
    margin-top: 1;
}

.ops-button-row {
    height: auto;
    align: left middle;
    padding: 0 0 1 0;
}

.ops-button-row Button {
    margin: 0 1;
    width: auto;
    min-width: 20;
}

#ops-status {
    height: 1;
    color: $text-muted;
    padding: 1 0;
}

#ops-output {
    height: 1fr;
    overflow-y: auto;
    border: solid $panel;
    padding: 1;
    min-height: 5;
}

/* Logs screen */
#logs-content {
    height: 1fr;
    overflow-y: auto;
}

#logs-title {
    padding: 1;
    text-style: bold;
}

/* Detail sections with spacing */
#event-desc,
#event-occurrences,
#event-tags,
#event-urls,
#event-sources,
#loc-desc,
#loc-meta,
#loc-alt-names,
#loc-tags,
#loc-events,
#source-meta,
#source-urls,
#source-config {
    padding: 0 0 1 0;
}

/* Dialog overrides */
ConfirmDialog > Static,
InputDialog > Static {
    padding: 2;
    border: thick $accent;
    background: $surface;
    color: $text;
}
